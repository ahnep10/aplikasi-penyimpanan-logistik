---
phase: 02-order-management-fulfillment
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [
  "app/Http/Controllers/Customer/CatalogController.php",
  "app/Http/Controllers/Customer/OrderController.php",
  "resources/views/customer/catalog/index.blade.php",
  "resources/views/customer/orders/create.blade.php",
  "resources/views/customer/orders/index.blade.php",
  "resources/views/customer/orders/show.blade.php",
  "routes/web.php"
]
autonomous: true
requirements: [ORD-01]

must_haves:
  truths:
    - "Customers can view a product catalog with current availability"
    - "Customers can place an order and see it in their order history"
    - "Customers only see their own orders"
  artifacts:
    - path: "app/Http/Controllers/Customer/OrderController.php"
      provides: "Customer-facing order management"
    - path: "resources/views/customer/orders/index.blade.php"
      provides: "Personal order history UI"
---

<objective>
Implement the Customer Portal, enabling SME customers to browse products and place orders independently.

Purpose: Reduce manual staff effort for order entry and provide transparency to customers.
Output: Customer Catalog, Ordering UI, and My Orders dashboard.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-order-management-fulfillment/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Customer Product Catalog</name>
  <files>
    app/Http/Controllers/Customer/CatalogController.php,
    resources/views/customer/catalog/index.blade.php,
    routes/web.php
  </files>
  <action>
    1. Create `CatalogController@index` for customers. It should fetch all active products and their aggregate stock levels.
    2. Create a clean Bootstrap-based catalog view showing product name, SKU, price, and "In Stock" status.
    3. Define customer routes under `customer.` prefix and `auth` middleware.
  </action>
  <verify>
    <automated>curl -u customer@example.com:password http://localhost/customer/catalog</automated>
  </verify>
  <done>Product catalog accessible to authenticated customers.</done>
</task>

<task type="auto">
  <name>Task 2: Customer Ordering Flow</name>
  <files>
    app/Http/Controllers/Customer/OrderController.php,
    resources/views/customer/orders/create.blade.php,
    resources/views/customer/orders/index.blade.php,
    resources/views/customer/orders/show.blade.php
  </files>
  <action>
    1. Implement `OrderController@create`: View to select quantity for products from catalog.
    2. Implement `OrderController@store`: Call `OrderService::placeOrder` with the authenticated customer.
    3. Implement `OrderController@index`: List orders for the authenticated customer only.
    4. Implement `OrderController@show`: Detailed order view.
  </action>
  <verify>
    <automated>php artisan test --filter=CustomerOrderTest</automated>
  </verify>
  <done>Customers can successfully place and track orders.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | Customer Order Controller | mitigate | Use Eloquent scopes or strict `auth()->user()->customer->orders()` checks to prevent ID-guessing attacks (IDOR). |
</threat_model>

<verification>
Login as a customer user, place an order, and verify it appears in "My Orders".
</verification>

<success_criteria>
- Catalog shows real-time availability.
- Order placement redirects to history with success message.
- "My Orders" list is restricted to the logged-in customer's data.
</success_criteria>

<output>
Create `.planning/phases/02-order-management-fulfillment/02-03-SUMMARY.md` when done
</output>
