---
phase: 02-order-management-fulfillment
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [
  "app/Http/Controllers/Staff/OrderManagementController.php",
  "app/Http/Controllers/Staff/FulfillmentController.php",
  "resources/views/staff/orders/create.blade.php",
  "resources/views/staff/orders/index.blade.php",
  "resources/views/staff/orders/show.blade.php",
  "resources/views/staff/orders/documents/packing-slip.blade.php",
  "resources/views/staff/orders/documents/invoice.blade.php"
]
autonomous: false
requirements: [ORD-02, ORD-03, ORD-05]

must_haves:
  truths:
    - "Staff can place orders on behalf of any customer"
    - "Order status can be transitioned through the lifecycle (Processing -> Dispatched -> Delivered)"
    - "Packing slips and Invoices are printable via browser"
  artifacts:
    - path: "app/Http/Controllers/Staff/OrderManagementController.php"
      provides: "Staff order entry and status control"
    - path: "resources/views/staff/orders/documents/packing-slip.blade.php"
      provides: "Print-optimized packing slip"
---

<objective>
Enable warehouse staff to manage the order lifecycle, from manual entry on behalf of customers to final fulfillment and document printing.

Purpose: Provide operational tools for staff to handle B2B fulfillment and logistics.
Output: Staff Order Entry UI, Order Lifecycle Dashboard, and Printable Documents.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-order-management-fulfillment/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Staff Order Entry</name>
  <files>
    app/Http/Controllers/Staff/OrderManagementController.php,
    resources/views/staff/orders/create.blade.php,
    resources/views/staff/orders/index.blade.php
  </files>
  <action>
    1. Implement `OrderManagementController@create`: Staff view to select a Customer and add products.
    2. Implement `OrderManagementController@store`: Call `OrderService::placeOrder` using selected customer ID.
    3. Implement `OrderManagementController@index`: List all orders in the system with filters for status and customer.
  </action>
  <verify>
    <automated>php artisan test --filter=StaffOrderEntryTest</automated>
  </verify>
  <done>Staff can create orders for customers and view the global order list.</done>
</task>

<task type="auto">
  <name>Task 2: Order Lifecycle & Status Management</name>
  <files>
    app/Http/Controllers/Staff/FulfillmentController.php,
    resources/views/staff/orders/show.blade.php
  </files>
  <action>
    1. Implement `FulfillmentController@updateStatus`: Allow transitioning order status (e.g., PENDING -> PROCESSING).
    2. Add "Transition" buttons to the Order Show view.
    3. Logic check: Ensure status transitions follow logical order (e.g., cannot go from CANCELLED to DELIVERED).
  </action>
  <verify>
    <automated>php artisan test --filter=OrderStatusTransitionTest</automated>
  </verify>
  <done>Order status can be managed through the full lifecycle.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Printable HTML Documents (Packing Slip & Invoice)</what-built>
  <how-to-verify>
    1. Open an Order Detail page as Staff.
    2. Click "Print Packing Slip" or "Print Invoice".
    3. Verify the layout looks professional in the browser print preview (Ctrl+P).
    4. Ensure no navigation menus or footers are visible in the print view (using @media print).
  </how-to-verify>
  <resume-signal>Approved: Documents are print-ready.</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Elevation of Privilege | Staff Order Controllers | mitigate | Ensure `RoleMiddleware` restricts these routes to 'staff' and 'admin' roles only. |
</threat_model>

<verification>
Create an order as staff, transition it to 'Dispatched', and print the packing slip.
</verification>

<success_criteria>
- Staff can search and select customers for order entry.
- Order details show a clear history of status changes.
- Printed documents contain essential info (Order #, Date, Items, Customer Address).
</success_criteria>

<output>
Create `.planning/phases/02-order-management-fulfillment/02-04-SUMMARY.md` when done
</output>
