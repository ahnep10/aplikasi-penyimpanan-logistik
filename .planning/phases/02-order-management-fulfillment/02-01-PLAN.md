---
phase: 02-order-management-fulfillment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "database/migrations/2026_06_12_000001_create_customers_table.php",
  "database/migrations/2026_06_12_000002_create_orders_table.php",
  "database/migrations/2026_06_12_000003_create_order_items_table.php",
  "database/migrations/2026_06_12_000004_add_batch_id_to_inventory_movements_table.php",
  "app/Models/Customer.php",
  "app/Models/Order.php",
  "app/Models/OrderItem.php",
  "app/Models/InventoryMovement.php",
  "app/Enums/OrderStatus.php"
]
autonomous: true
requirements: [ORD-01, ORD-03]

must_haves:
  truths:
    - "Customer, Order, and OrderItem tables exist in the database"
    - "InventoryMovements table has a nullable batch_id column for FIFO tracking"
    - "Models have defined relationships (Customer -> Orders, Order -> Items, Order -> Customer)"
  artifacts:
    - path: "app/Models/Customer.php"
      provides: "Customer business data linked to User"
    - path: "app/Models/Order.php"
      provides: "Order header and status tracking"
    - path: "app/Enums/OrderStatus.php"
      provides: "Type-safe order status values"
---

<objective>
Establish the core database schema and Eloquent models for Order Management and Customer identities.

Purpose: Provide the persistent foundation for order lifecycle and FIFO inventory tracking.
Output: Migrations, Models, and Status Enums.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-order-management-fulfillment/02-CONTEXT.md
@.planning/phases/02-order-management-fulfillment/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database Migrations for Order Entities</name>
  <files>
    database/migrations/2026_06_12_000001_create_customers_table.php,
    database/migrations/2026_06_12_000002_create_orders_table.php,
    database/migrations/2026_06_12_000003_create_order_items_table.php,
    database/migrations/2026_06_12_000004_add_batch_id_to_inventory_movements_table.php
  </files>
  <action>
    Create migrations following Phase 1 naming and structure patterns:
    1. `customers`: id, user_id (FK to users), name, business_address, phone, email, timestamps.
    2. `orders`: id, customer_id (FK), status (string, default: pending), total_amount (decimal 15,2), shipping_address (text), created_by (FK to users), timestamps.
    3. `order_items`: id, order_id (FK), product_id (FK), quantity (int), unit_price (decimal 15,2), subtotal (decimal 15,2), timestamps.
    4. `inventory_movements`: Add nullable `batch_id` (FK to inventory_batches) column.
    
    Run `php artisan migrate`.
  </action>
  <verify>
    <automated>php artisan migrate:status</automated>
  </verify>
  <done>Database schema updated with new tables and columns.</done>
</task>

<task type="auto">
  <name>Task 2: Define Models and Status Enum</name>
  <files>
    app/Models/Customer.php,
    app/Models/Order.php,
    app/Models/OrderItem.php,
    app/Models/InventoryMovement.php,
    app/Enums/OrderStatus.php
  </files>
  <action>
    1. Create `app/Enums/OrderStatus.php` as a Backed Enum (string): `PENDING`, `PROCESSING`, `DISPATCHED`, `DELIVERED`, `CANCELLED`.
    2. Create `Customer` model: $fillable for name, business_address, phone, email, user_id. Define `user()` and `orders()` relationships.
    3. Create `Order` model: $fillable for customer_id, status, total_amount, shipping_address, created_by. Cast status to `OrderStatus::class`. Define `customer()`, `items()`, and `creator()` relationships.
    4. Create `OrderItem` model: $fillable for order_id, product_id, quantity, unit_price, subtotal. Define `order()` and `product()` relationships.
    5. Update `InventoryMovement` model: Add `batch_id` to $fillable. Define `batch()` relationship.
  </action>
  <verify>
    <automated>php artisan tinker --execute="echo App\Models\Order::class; echo App\Enums\OrderStatus::PENDING->value;"</automated>
  </verify>
  <done>Models and Enums correctly defined and linked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> Customer | Authenticated User must be authorized to access Customer business data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Order Migration | mitigate | Ensure strict foreign key constraints in migrations. |
| T-02-02 | Information Disclosure | Customer Model | mitigate | Link Customer to User for strict RBAC in subsequent plans. |
</threat_model>

<verification>
Run `php artisan migrate` and verify table existence via `php artisan tinker`.
</verification>

<success_criteria>
- Customers, Orders, and OrderItems tables exist.
- `inventory_movements` table has `batch_id`.
- Eloquent models allow traversing relationships between Customers and Orders.
</success_criteria>

<output>
Create `.planning/phases/02-order-management-fulfillment/02-01-SUMMARY.md` when done
</output>
