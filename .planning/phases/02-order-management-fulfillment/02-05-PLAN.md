---
phase: 02-order-management-fulfillment
plan: 05
type: execute
wave: 4
depends_on: ["02-03", "02-04"]
files_modified: [
  "app/Services/InventoryService.php",
  "app/Http/Controllers/DashboardController.php",
  "resources/views/dashboard.blade.php",
  "resources/views/components/low-stock-alert.blade.php"
]
autonomous: true
requirements: [INV-05]

must_haves:
  truths:
    - "Products falling below safety_stock trigger a visible alert on the dashboard"
    - "Dashboard shows current order fulfillment KPIs"
  artifacts:
    - path: "resources/views/components/low-stock-alert.blade.php"
      provides: "Reusable UI component for stock warnings"
---

<objective>
Finalize Phase 2 by implementing low-stock alerts and updating the management dashboard with order-related KPIs.

Purpose: Provide managers with actionable visibility into inventory health and fulfillment performance.
Output: Low-stock Alert logic, Dashboard Widgets, and Phase 2 Verification.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-order-management-fulfillment/02-03-SUMMARY.md
@.planning/phases/02-order-management-fulfillment/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Low-Stock Alert Logic</name>
  <files>
    app/Services/InventoryService.php,
    app/Http/Controllers/DashboardController.php,
    resources/views/components/low-stock-alert.blade.php
  </files>
  <action>
    1. Implement `InventoryService::getLowStockProducts()`: Returns products where aggregate stock < `safety_stock`.
    2. Create `low-stock-alert` Blade component to display a table of at-risk products.
    3. Update `DashboardController` to fetch low-stock data for staff/admin users.
  </action>
  <verify>
    <automated>php artisan tinker --execute="print_r(app(App\Services\InventoryService::class)->getLowStockProducts()->toArray());"</automated>
  </verify>
  <done>Low-stock alerts dynamically calculated and available for UI.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard KPI Updates</name>
  <files>
    resources/views/dashboard.blade.php,
    app/Http/Controllers/DashboardController.php
  </files>
  <action>
    1. Add KPI cards to Dashboard: "Total Active Orders", "Pending Fulfillment", "Low Stock Items Count".
    2. Ensure KPIs are scoped by role (e.g., customers see only their own order count).
    3. Include the `low-stock-alert` component on the staff/admin dashboard view.
  </action>
  <verify>
    <automated>curl -u admin@example.com:password http://localhost/dashboard | grep "Low Stock"</automated>
  </verify>
  <done>Dashboard provides a comprehensive overview of Phase 2 activities.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 2 Final Verification</name>
  <files>tests/Feature/Phase2FulfillmentTest.php</files>
  <action>
    Create a comprehensive feature test that:
    1. Seeds a product with 10 units in 2 batches.
    2. Places an order for 8 units.
    3. Verifies stock is correctly deducted (FIFO).
    4. Checks that a low-stock alert is now active for that product.
    5. Transitions order to 'Delivered'.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase2FulfillmentTest</automated>
  </verify>
  <done>All Phase 2 requirements verified as integrated and working.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | Dashboard Controller | mitigate | Ensure aggregate counts in Dashboard follow the same RBAC/Ownership logic as the detail views. |
</threat_model>

<verification>
Review Dashboard as Admin and verify alerts appear when products are manually adjusted below safety stock.
</verification>

<success_criteria>
- Dashboard shows accurate order counts.
- Low-stock alert widget highlights products needing replenishment.
- All Phase 2 feature tests pass.
</success_criteria>

<output>
Create `.planning/phases/02-order-management-fulfillment/02-05-SUMMARY.md` when done
</output>
